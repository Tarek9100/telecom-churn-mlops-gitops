apiVersion: v1
kind: Service
metadata:
  name: churn-api
  namespace: churn
  labels:
    app: churn-api
spec:
  type: NodePort
  selector:
    app: churn-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
      nodePort: 30080
